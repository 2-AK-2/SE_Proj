{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "lint": "eslint . --ext .js --format json --output-file lint-report.json",
    "coverage": "jest --coverage --coverageReporters='html' --coverageReporters='json-summary' --coverageThreshold='{\"global\": {\"lines\": 75}}' ",
    "security": "npm audit --json > security-report.json"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.1.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "superagent": "^10.2.3",
    "supertest": "^6.3.4"
  }
}
