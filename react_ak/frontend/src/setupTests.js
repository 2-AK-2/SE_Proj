// jest-dom adds custom jest matchers for asserting on DOM nodes.
import '@testing-library/jest-dom';

// We do NOT need to mock axios globally here if we do it in the test files
// or use a __mocks__ folder. Keeping this file clean prevents "double mocking" issues.